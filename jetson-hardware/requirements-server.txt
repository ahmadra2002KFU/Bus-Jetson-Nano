# Server-side Python dependencies (FastAPI app in Docker).
# fastapi 0.115.x pins starlette<1.0; pinning here avoids surprise 1.0 breakage.
fastapi>=0.115,<0.116
starlette>=0.40,<1.0
uvicorn[standard]>=0.27
aiosqlite>=0.19
jinja2>=3.1
python-multipart>=0.0.9
